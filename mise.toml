[tools]
actionlint = "1.7.12"
"pipx:zizmor" = "1.30.1"

[tasks.verify]
description = "Lint and audit the shared workflows"
sources = [".github/**/*.yml"]
run = '''
actionlint
token="${GH_TOKEN:-$(gh auth token)}" || exit 1
GH_TOKEN="$token" zizmor .github/workflows
git diff --check
'''
